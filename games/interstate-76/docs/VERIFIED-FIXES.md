# Interstate '76 on Apple Silicon — every verified fix, one table

*The fast-discovery index for future travelers. Each row was **hit, diagnosed, and fixed (or
measured) on this port** — not speculation. Deep dives live in the linked docs; this page is the
map. Stack: GOG Gold (2019 exe) + Sikarugir Wine 10 (wow64) wrapper, Apple Silicon, no CrossOver.*

## Crashes / freezes / hangs

| Symptom | Root cause | Fix |
|---|---|---|
| Page fault at `01B82C26` at boot | Game calls `NtUserChangeDisplaySettings`; macOS refuses exclusive 640x480; game derefs null | Per-app Wine **virtual desktop**: `HKCU\Software\Wine\AppDefaults\i76.exe\Explorer → Desktop=i76`, `HKCU\Software\Wine\Explorer\Desktops → i76=1280x960` |
| Multi-second freezes in menus/missions, then **stack-overflow crash**; also silent mission music | Launch env missing **GStreamer plugin paths** → winegstreamer can't decode MP3 → MCI open fails → game retries in a tight loop | Set `GST_PLUGIN_PATH`, `GST_PLUGIN_SYSTEM_PATH_1_0`, `GST_PLUGIN_SCANNER_1_0`, `GST_REGISTRY_1_0` + GStreamer libs on `DYLD_FALLBACK_LIBRARY_PATH` (see [i76-launch-stub.swift](../i76-launch-stub.swift)) |
| Crash at `i76+0x4507C` in `-glide` (OpenGLide) when launched by the stock wrapper launcher | Stock Sikarugir launcher exports `CX_FWD_COMPAT_GL_CTX=1` (forward-compatible GL context kills legacy GL) **and** sets a wrong CWD (hides `OpenGLid.INI`/`dgVoodoo.conf`, which are CWD-discovered) | Replace the launcher with our Mach-O stub (env + correct CWD) |
| Every Wine process pinned at **100% CPU** | `WINEESYNC=1` alone busy-spins on macOS | Set **both** `WINEESYNC=1` and `WINEMSYNC=1` (msync wins; all procs idle at ~0-4%) |
| dgVoodoo crash at present-time (`0x79F119BD`) with dgVoodoo ≥ 2.81 | 2.81+ adapter-enumeration regression under Wine (dxvk#5217 / wine bug 58731) | Use **dgVoodoo 2.78.2** |
| Defensive: 1 MB stack is tight for this engine under Wine | 1997-era `SizeOfStackReserve` | PE patch OptionalHeader+0x48 → 8 MB (backup kept: `i76.exe.pre-stackpatch`) |

## Window / display

| Symptom | Root cause | Fix |
|---|---|---|
| Window draws once then **vanishes to (-16000,-16000)** on any focus loss | Wine hardcodes minimize-on-focus-loss for exclusive-fullscreen ddraw (since 1.7.32; no off-switch) | Don't run exclusive fullscreen: DxWnd windowed path. Mitigation if you must: `HKCU\Software\Wine\Mac Driver → WindowsFloatWhenInactive=all` |
| Tiny fixed 640x480 window (`-gdi`) | That renderer doesn't scale | **DxWnd** (default): scales the software render into a big resizable title-barred window |
| Fullscreen-ish stretch instead of 4:3 | Wrong DxWnd position mode | Profile: `coord0=3` (Desktop) + **Keep aspect ratio** (`flagg0` bit 0x10) + Hide desktop (`flagi0` bit 0x8000) → letterboxed 4:3 filling the screen. `Adaptive ratio` = stretch, leave OFF. Video-tab "Fix aspect ratio" is NOT letterboxing. See [DXWND-TUNING.md](DXWND-TUNING.md) |
| Black menus/cutscenes under DxWnd | Emulated blit renderers | DirectX tab **Renderer = primary surface** (`renderer0=3`) — note: upscale filters only work on emulated renderers, so filters and this fix are in tension |
| **Black "wine" window survives quitting; force-quit needed** | Two-part: DxWnd host stays resident after the game dies (so a launcher waiting on `dxwnd.exe` never reaps) + the HideDesktop backdrop ("hider" window) belongs to processes `wineserver -k` alone may miss | Stub waits for **`i76.exe`** to exit (not dxwnd), then `wineserver -k` + `pkill -f <bundle>/Contents/SharedSupport` sweep. Verified: 0 processes, 0 windows within 10 s |
| **Finder: "You can't open the application … because it is not responding" on relaunch** | The previous session didn't fully die: the launcher stub lingers in its watch loop (LaunchServices still counts the app as open) and/or a wedged `wineboot.exe --init` holds the prefix so a new launch can't boot. Typically after a quit that interrupted boot, or launching again too quickly | `pkill -f "Interstate 76 - Software (DxWnd).app"` (kills stub + all wrapper wine procs), wait ~2 s, launch again — boots instantly. Prevention: give the quit watchdog ~10 s to sweep before relaunching |

## Launch plumbing (macOS specifics)

| Symptom | Root cause | Fix |
|---|---|---|
| DxWnd GUI opens instead of the game with `/r:0` | `/R` is **1-based** (source: `iProgIndex-1`) | `dxwnd.exe /R:1` for headless one-click launch. Do **not** add `/q` — it suppresses the launch |
| Blank never-painting window when stub `execv`s wine | `execv` breaks winemac GUI activation under LaunchServices | Spawn wine as a **child Process** from the stub |
| .app won't launch from Finder with a script as the executable | LaunchServices refuses script bundle executables | Compile a small **Mach-O** stub |
| `codesign --deep` fails on the wrapper | Chokes on internal symlinks | Sign the stub binary alone |
| macOS asks for **microphone permission** at launch | The game/Wine opens a capture stream at audio init (winecoreaudio starts every capture stream at creation — `AudioOutputUnitStart` — which is what TCC gates; enumeration alone doesn't prompt). **No Wine registry key/env/DllOverride exists to disable capture while keeping output** (verified against Wine 10 source; the only knob, `Drivers\Audio=""`, kills ALL audio) | **Deny once — it's permanent** (TCC stores it per bundle ID; denied capture just reads silence, output unaffected). It re-prompts once after the wrapper binary is re-signed (our stub rebuilds do that) — deny again |

## Audio / music

| Symptom | Root cause | Fix |
|---|---|---|
| Music in cutscenes but **silent in missions** | Mission music = CD **redbook audio**; GOG ships it as `music/N.mp3` + an *empty* `tracklen.nfo`; no CD to play under Wine | DxWnd **virtual CD audio** (`flagm0` bit 0 → dxwplay.dll) + hard-link tracks to its expected `Music\TrackNN.mp3` naming + delete the empty `tracklen.nfo` (it regenerates). Script: [setup-music.sh](../setup-music.sh). Requires the GStreamer env above. **Confirmed in-mission** |
| **Wrong music over FMV cutscenes** (mission/office track bleeds over the movie's own chill score) | **Known GOG bug, NOT the port.** The 29 `smk/*.SMK` cutscenes carry their own baked audio (voice + Salazar score, Smacker audio track 1 — verified in the headers). On the real CD, the single-head drive *couldn't* stream data + redbook at once, so reading the movie stopped the music automatically; the devs relied on that and never coded an explicit stop. GOG's file-based music (our virtual CD) has no such limit, so the last track keeps playing over the movie. Happens identically on GOG-for-Windows; the AiO patch doesn't fix it either. [GOG thread](https://www.gog.com/forum/interstate_series/i76_music_plays_during_cutscenes) | **FIXED — our `SMACKW32.DLL` proxy** ([smack-music-fix/](../smack-music-fix/), install: [setup-cutscene-music-fix.sh](../setup-cutscene-music-fix.sh)). Recreates the hardware behavior at the same trigger: on `SmackOpen` (= movie start) it broadcasts `MCI_STOP` to every open MCI device on every plausible layer (static winmm import → DxWnd's dxwplay on Mac; `win32.dll` audiere shim on GOG-Windows), then forwards to the renamed original `smackorg.dll`. **The exe imports SMACKW32 BY ORDINAL** — the proxy's 39 exports are ordinal-exact (14 = `_SmackOpen@12`); freestanding build (KERNEL32+WINMM only, no CRT). Mission music untouched (the game starts each track itself). Fallbacks if ever needed: `setup-music.sh --off` (silence CD music entirely) or Abort→Replay. Revert: `setup-cutscene-music-fix.sh --revert` |

## Input

| Symptom | Root cause | Fix |
|---|---|---|
| Mac arrow keys glance/track camera instead of steering | winemac delivers Mac arrows as the game's `Grey*` (numpad) codes; stock `KEYBOARD.MAP` puts driving on plain-arrow names Macs never send | [fix-arrows-for-mac.sh](../fix-arrows-for-mac.sh) swaps the four arrow tokens in `KEYBOARD.MAP` |
| Mouse clicks land in the wrong place in menus | Game hit-tests in internal 640x480 coords; scaling paths (virtual desktop, DxWnd) offset it | Navigate menus by keyboard (arrows + Enter). Synthetic/automated clicks are ignored entirely through DxWnd scaling |
| Want mouse steering/throttle + mouse-button weapons | I76 has **native mouse support**: analog channels `mouse Left/Right`, `mouse Down/Up`; buttons `LeftBtn/RightBtn/MiddleBtn` (exactly three exist in the engine — weapon 4 stays on a key) | [setup-mouse-and-pad.sh](../setup-mouse-and-pad.sh) patches `input.map`. NOTE: `input.map` is the file the engine reads (KEYBOARD.MAP/JOYSTICK.MAP are shell templates); within a block, multiple `+` lines are a CHORD; alternative bindings = separate blocks |
| **Mouse STEERING doesn't actually work under DxWnd** (bindings present, car won't steer) | I76 reads the mouse via **`GetCursorPos`** (absolute cursor position, confirmed in the exe — no DirectInput), so steering needs the cursor **captured/clipped** to the window to give continuous deflection. Under DxWnd + winemac the cursor isn't reliably clipped (it escapes the window; same coordinate-virtualization family as the menu-click offset) | **Known DxWnd-path limitation.** The `mouse Left/Right` binding is correct; the delivery is the problem. Lever to try (unverified — needs in-sim testing): a DxWnd cursor **lock/clip** option, or winemac cursor-clip. Works on the **Steam Deck** (native path). Use keyboard A/D or a gamepad stick meanwhile |
| Rebind keys (e.g. Space=handbrake, weapons on `,`/`.`) | `input.map` supports it directly; valid key tokens include `Enter Minus Equal Comma Period LeftBracket RightBracket` + letters/numbers (`Control`/`Shift` are modifiers, not primaries) | Edit `input.map` (back up first). Multiple blocks for one action = alternative bindings. Example applied: Space→`e_brake`, `weapon_fire`→Enter, `hardpoint3/4`+=`Comma`/`Period`, gear-shift moved off `,`/`.` to `-`/`=` to avoid the double-trigger |
| Xbox controller does nothing | GOG shipped `input.map` bound to phantom **joystick5** (packager's machine); under Wine the first pad is joystick1. The game is winmm-only (`joyGetPosEx` — no DirectInput), which Wine 10 serves via winebus→dinput8→winmm; this prefix already enumerated an Xbox Wireless Controller (045E:0B20) | Same script fixes joystick5→joystick1 + binds left stick/buttons/hat. **Connect the pad BEFORE launching** (winmm-era games enumerate at startup only). Verify visibility: `wine control joy.cpl`. Triggers share one axis (`Throttle`/Z); right stick mostly unusable natively |
| Rebinding via the in-game Control Configuration menu breaks bindings | Menu appends chords instead of replacing, binds wrong stick numbers; crash reports (GOG forum, VOGONS) | **Never use the in-game menu** — edit `input.map` directly |
| Force feedback (wheel) silent | Game FFB is real (Nitro Pack, in GOG Gold, off by default) but Wine's only FFB backend is **Linux evdev** — no macOS path exists | No Mac fix. On a Windows box: run [enable-force-feedback.bat](../enable-force-feedback.bat) (registry key copy) and FFB works on modern wheels. See [FORCE-FEEDBACK-AND-VISUALS.md](FORCE-FEEDBACK-AND-VISUALS.md) |

## Performance / visuals (measured ceilings)

| Fact | Detail |
|---|---|
| **20 FPS cap ships in the GOG exe** | GOG 2019 `i76.exe` is byte-identical to UCyborg's AiO patch; `I76PATCH.DLL` = built-in 20 FPS physics limiter. **Measured ~20.66 FPS in-sim** — physics-safe with no external limiter. So the DxWnd limiter below only *bites* when set **under ~20.66** |
| **DxWnd FPS limiter: how to actually set it (was silently off)** | Two parts, BOTH required — the value alone does nothing: **(1) enable** = `flagg0` bit **`0x1000`** (LIMITFPS, the "Limit frame rate" checkbox); **(2) value** `maxfps0` = **delay in ms** when the "Hz" flag (`0x4000`, LIMITFREQUENCY) is **off** (our mode), with `FPS = 1000/ms`. So **50 ms = 20 FPS, 51 ms ≈ 19.6, 52 ms ≈ 19.2** (whole-ms granularity — 19.5 exactly isn't expressible). Decoded from DxWnd's own example profiles + `help/Timing.html`. Current shipped: **`maxfps0=52` (≈19.2 FPS)**, `flagg0=1543507984` — set deliberately a touch under 20 so the Mission 5 canyon jump *and* the tighter later-mission bridge gaps (tuned for the era's exact-20 sim) clear reliably. Both the live `drive_c/dxwnd/dxwnd.ini` (profile 0) and the repo `interstate-76.dxw` must be edited (CRLF files — anchor edits at line start) |
| **1024x768 = software renderer max** | Menu maxes there because the engine does; no config/registry/hex unlock found; D3D mode is locked 640x480. DxWnd upscales output beyond it |
| **Default is 640x480; set 1024x768 in-game** | The software renderer's resolution lives in `I76PLYR.DEF` (binary player-prefs, no readable dimensions — it's an index byte). Set it via **Options → Graphic Detail → Screen Resolution → 1024x768**; it persists (the close-fix now saves cleanly on quit). Can't be baked as a shipped default headlessly: the 2D menu renders **black to `screencapture`** under DxWnd's primary-surface renderer (3D captures fine, menu doesn't), so no before/after diff to locate the byte. To bake it: set it once, then diff `I76PLYR.DEF` vs a 640x480 baseline |
| Camera is hardcoded 4:3 | No widescreen exists; "fill screen" = big 4:3 + black bars |
| Higher *internal* res needs Glide | dgVoodoo 2.78.2 (Glide→D3D11→DXVK→MoltenVK) renders 2x+ with 3dfx gamma — the "Voodoo" launcher. Cost: pipeline-compile warmup on first-seen content (SPIRV→MSL); mitigations: DXVK async + state cache (warm after a break-in run); true fix in progress: persist the VkPipelineCache |
| **HD pack: night missions (e.g. Mission 6) color-shifted; day worlds fine** | The pack ships night only as software **VQM** (`nightm.pak`), and VQM is indexed against each level's own 8-bit `.ACT` palette (no per-tile palette like Glide M16) — the pack's night VQM was quantized against the wrong palette. Stock ADDON had no night files, so it's a pack artifact, not a game bug. **Fix:** `setup-mac-hd-textures.sh` now excludes the night VQM (stashes it in `ADDON/.night-hd-disabled/`); night falls back to correct stock textures, day worlds keep the HD upgrade. Real cure = re-quantize night against its `.ACT` on the Windows box. See [HD-TEXTURES-RESEARCH.md](HD-TEXTURES-RESEARCH.md) |
| dgVoodoo under Wine needs 3 conditions | ≤2.78.2 + **DXVK** d3d11 (wined3d refuses FL10.1 on GL and Vulkan) + wrap **Glide only** (wrapping ddraw too = dual swapchains stacking invisibly in winemac). Full saga: [DXGI-DGVOODOO-RESEARCH.md](DXGI-DGVOODOO-RESEARCH.md) |

## Gameplay quirks (engine, not the port)

| Symptom | Root cause | Fix |
|---|---|---|
| **Mission 5 canyon jump falls just short** (no other way out) | I76's jump distance is **inversely tied to framerate** - above 20 FPS the car falls too fast per frame and lands short ([Local Ditch](https://www.localditch.com/posts/fps-jumping/), [GOG forum](https://www.gog.com/forum/interstate_series/argh_help)). The GOG exe's AiO limiter targets 20 but overshoots to ~**20.66** here - just enough to miss | Cap to exactly 20: DxWnd Timing tab: check **Limit** and set **delay (msec) = 50** (the field is *ms-per-frame* = 1000/FPS, so 20 FPS = 50 ms — NOT `maxfps0=20`, which is 50 FPS!). Enabling the Limit checkbox is required; the value alone does nothing. Throttles the ddraw Flip -> the game loop incl. physics runs at 20. Then **use nitrous** on the ramp approach for margin |
| **Nitrous equipped but nothing happens** | Nitrous is a **mounted "special"**, not a "nitrous key" - `nitrous_on`/`nitrous_off` are internal engine states, NOT bindable input actions (confirmed: they sit in a different exe string table from the real actions). Activated via a **special slot** (`special1/2/3`) | Bind all three special slots to keys (this build left `special1` on a joystick button only). We set `special1=6` (the stock default), `special2=7`, `special3=8`; nitrous fires from whichever slot it's mounted in. See [I76-GAMEPLAY-REFERENCE.md](I76-GAMEPLAY-REFERENCE.md) |

## Where everything lives

- Launchers: [build-launchers.sh](../build-launchers.sh) → `Interstate 76 - Software (DxWnd).app` (the Mac build), `Interstate 76 - DxWnd Settings.app` (GUI). Voodoo/Glide is **parked** ([VOODOO-PARKED.md](VOODOO-PARKED.md))
- DxWnd: [setup-dxwnd.sh](../setup-dxwnd.sh) + profile [interstate-76.dxw](../interstate-76.dxw); settings map: [DXWND-TUNING.md](DXWND-TUNING.md)
- Research compendia: [RUNNING-I76-EVERYWHERE.md](RUNNING-I76-EVERYWHERE.md), [DXGI-DGVOODOO-RESEARCH.md](DXGI-DGVOODOO-RESEARCH.md), [FORCE-FEEDBACK-AND-VISUALS.md](FORCE-FEEDBACK-AND-VISUALS.md)
