#!/bin/sh
# Interstate '76 - one mode, one way in. The wrapper's executable is our
# direct-launch shim (see app-shim.sh), so this is just:
exec open "$HOME/Applications/Sikarugir/Interstate 76 - Software (DxWnd).app"
